import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderColor: 'black',
    borderWidth: '10px',
    margin: '10px',
  },
  viewport: {
    height: '200px',
    backgroundColor: 'teal'
  }
});
